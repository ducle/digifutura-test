$('#sharingModal .modal-content').html("#{escape_javascript(render partial: 'sharing_modal', locals: {file_node: @file_node}) }");
$('#sharingModal').modal('show');

bindTokenInput('#tokenfield', #{raw User.where.not(id: current_user.id).map{|u| {id: u.id, name: u.name}}.to_json}, #{raw @file_node.users.map{|u| {id: u.id, name: u.name}}.to_json})
